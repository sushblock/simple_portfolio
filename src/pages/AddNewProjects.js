import React from "react";
import { projectList } from "../data/data";
import "../App.css";
import "../styles/ProjectDisplay.css";

function AddNewProjects() {
  //authenticate with Firebase and only if user is admin then allow to add new projects
  //if not admin then redirect to home page
  //if admin then show form to add new projects

    

  //create a form to add new projects
  //form should have fields for projName, description, image
  //form should have a submit button
  //on submit, add new project to projectList array
  //redirect to projects page



  return <div className="project">

  </div>;
}

export default AddNewProjects;
